import { Container } from '@material-ui/core';

const Travel = () => {
    return (
        <div>
            <Container maxWidth='lg'>Travel page is here</Container>
        </div>
    );
};

export default Travel;
