import { Container } from '@material-ui/core';

const Health = () => {
    return (
        <div>
            <Container maxWidth='lg'>Health page is here</Container>
        </div>
    );
};

export default Health;
