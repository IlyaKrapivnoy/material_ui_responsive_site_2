import { Container } from '@material-ui/core';

const BusinessPosts = () => {
    return (
        <div>
            <Container maxWidth='lg'>Business posts</Container>
        </div>
    );
};

export default BusinessPosts;
