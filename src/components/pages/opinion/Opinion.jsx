import { Container } from '@material-ui/core';

const Opinion = () => {
    return (
        <div>
            <Container maxWidth='lg'>Opinion page is here</Container>
        </div>
    );
};

export default Opinion;
