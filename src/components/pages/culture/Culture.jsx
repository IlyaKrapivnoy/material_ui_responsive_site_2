import { Container } from '@material-ui/core';

const Culture = () => {
    return (
        <div>
            <Container maxWidth='lg'>Culture page is here</Container>
        </div>
    );
};

export default Culture;
